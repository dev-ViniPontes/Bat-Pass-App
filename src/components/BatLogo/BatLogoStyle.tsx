import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    title: {
        textAlign: "center",
        color: "#000",
        fontSize: 30,
        fontWeight: "bold",
    },
    image:{
        resizeMode: "contain",
        width: 200,
        height: 200
    }
});